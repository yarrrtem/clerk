{
  "mcpServers": {
    "todoist": {
      "command": "npx",
      "args": ["-y", "@doist/todoist-ai"],
      "env": {
        "TODOIST_API_KEY": "${TODOIST_API_KEY}"
      }
    },
    "linear": {
      "type": "http",
      "url": "https://mcp.linear.app/mcp"
    },
    "granola": {
      "command": "{{clerk_path}}/tools/granola-mcp/venv/bin/python",
      "args": ["-m", "granola_mcp.mcp"],
      "env": {
        "GRANOLA_CACHE_PATH": "${HOME}/Library/Application Support/Granola/cache-v3.json"
      }
    },
    "headless-browser": {
      "command": "{{clerk_path}}/tools/headless-browser/venv/bin/python",
      "args": [
        "{{clerk_path}}/tools/headless-browser/server.py"
      ]
    },
    "featurebase": {
      "command": "featurebase-mcp",
      "env": {
        "FEATUREBASE_API_KEY": "${FEATUREBASE_API_KEY}",
        "FEATUREBASE_ORG_URL": "${FEATUREBASE_ORG_URL}"
      }
    },
    "amplemarket-kb": {
      "command": "node",
      "args": [
        "{{clerk_path}}/tools/pylon-mcp/dist/src/index.js"
      ],
      "env": {
        "PYLON_API_TOKEN": "${PYLON_API_TOKEN}"
      }
    }
  }
}
